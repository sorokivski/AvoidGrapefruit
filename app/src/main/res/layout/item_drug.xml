<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:foreground="?attr/selectableItemBackground">

    <RelativeLayout
        android:id="@+id/drug_item_root"
        android:layout_width="match_parent"
        android:layout_height="130dp"
        android:padding="12dp">

        <!-- Clickable Drug Name (for fetching real drug info) -->
        <TextView
            android:id="@+id/drug_name"
            android:layout_width="233dp"
            android:layout_height="49dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginStart="12dp"
            android:layout_marginTop="13dp"
            android:clickable="true"
            android:focusable="true"
            android:text="name"
            android:textColor="@color/grapefruit_red"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- User-specific dosage info -->

        <!-- Intake times -->


        <TextView
            android:id="@+id/drug_dosage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/drug_name"
            android:layout_alignParentStart="true"
            android:layout_marginStart="27dp"
            android:layout_marginTop="11dp"
            android:text="amount - intake Time"
            android:textColor="@color/text_grey"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/drug_intake_times"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/drug_dosage"
            android:layout_alignParentStart="true"
            android:layout_marginStart="24dp"
            android:layout_marginTop="11dp"
            android:text="intake time"
            android:textColor="@color/accent_green"
            android:textSize="12sp" />

        <ImageButton
            android:id="@+id/btnMoreOptions"
            android:layout_width="66dp"
            android:layout_height="65dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_gravity="center_vertical"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="more options"
            android:padding="8dp"
            android:src="@drawable/ic_more_vert" />


    </RelativeLayout>
</androidx.cardview.widget.CardView>
